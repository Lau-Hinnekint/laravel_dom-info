<?php

use App\Http\Controllers\ProductController;
use App\Http\Controllers\ProfileController;
use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider and all of them will
| be assigned to the "web" middleware group. Make something great!
|
*/

Route::get('/', function () {
    return view('index');
});

Route::get('product-list', [ProductController::class, 'listProduct'])->name('productList');
Route::get('product-detail', [ProductController::class, 'detailProduct'])->name('productDetail');
Route::post('product-filter', [ProductController::class, 'filterProduct'])->name('productFilter');


/*/**
 * GENERATED BY BREEZE FOR AUTH 
 */ 

Route::get('/dashboard', function () {                      //'middleware' is used to filter the HTTP request 'auth' checked if someone is connecter and 'verified' check if its someone with verified email.
    return view('dashboard');                               
})->middleware(['auth', 'verified'])->name('dashboard');    //'auth' checked if someone is connecter and 'verified' check if its someone with verified email.

Route::middleware('auth')->group(function () {
    Route::get('/profile', [ProfileController::class, 'edit'])->name('profile.edit');
    Route::patch('/profile', [ProfileController::class, 'update'])->name('profile.update');
    Route::delete('/profile', [ProfileController::class, 'destroy'])->name('profile.destroy');
});

require __DIR__.'/auth.php';